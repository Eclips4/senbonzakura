data Vec2:
    x: Int
    y: Int

impl Add[Vec2, Vec2, Vec2]:
    def add(self: Vec2, other: Vec2) -> Vec2:
        return Vec2(self.x + other.x, self.y + other.y)

def double[T: Add](x: T) -> T:
    return x + x

let a: Int = double(21)
let b: Vec2 = double(Vec2(1, 2))
print("double 21 works")
